kind = "web"
previewPath = "/"
title = "AI Codebase Platform"
version = "1.0.0"
id = "artifacts/codebase-ui"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19556

[services.development]
run = "pnpm --filter @workspace/codebase-ui run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/codebase-ui", "run", "build" ]
publicDir = "artifacts/codebase-ui/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19556"
BASE_PATH = "/"
